# POC seed — gatekeeper stream

Review ungated drafts in `revenue-agents/outbox/` tagged `#poc-fixture` or dated today.

For each draft found:
1. Apply gate rubric from the gatekeeper charter
2. Append `## GATE VERDICT` with APPROVED or REJECTED and reason
3. Do not publish or send externally

If no drafts exist, write a single note file explaining zero drafts found (still mark `#poc-fixture`).
