# Growth OS POC fixtures

Used when `GROWTH_POC_MODE=true` in `growth/.env`.

| Path | Purpose |
|------|---------|
| `leads/` | Fictional prospect research bundles (no Apollo, no live scrapers) |
| `streams/<name>/context.md` | Seed context for non-leads charter runs |

All domains use `.example` TLDs. Nothing in this tree should trigger live outreach or publishing.
